version: 2
projects:
  - dir: project1
    workflow: myworkflow
workflows:
  myworkflow:
    plan:
      steps:
        - init
        - plan
        - run: echo "Goodbye, world!"
    apply:
      steps:
        - apply
        - run: echo "Goodbye, world"

